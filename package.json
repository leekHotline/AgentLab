{
  "name": "agentlab",
  "version": "1.0.0",
  "description": "Multi-agent collaboration and scheduling system",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "keywords": [
    "multi-agent",
    "collaboration",
    "scheduling",
    "orchestration"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
